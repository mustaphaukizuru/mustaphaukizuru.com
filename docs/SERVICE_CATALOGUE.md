# Retired — see `docs/catalogue/`

This was the v1.0 catalogue draft (July 2026), written before the closed set of
four categories existed. It is kept only so older links resolve.

The source of truth is **`web/src/data/servicesCatalogue.js`**. The readable
form is generated from it into [`docs/catalogue/`](catalogue/) — see
[`SERVICE_CATALOGUE_2026-08.md`](SERVICE_CATALOGUE_2026-08.md) for the index
and the regeneration command.

The original content is in the git history — `git log --follow` this path.
