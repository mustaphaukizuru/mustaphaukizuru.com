/* eslint-disable react-refresh/only-export-components -- thin re-export barrel (canonical impl lives in components/ui) */
// Stat · thin re-export — canonical implementation lives in components/ui/Stat.jsx
export { default, Stat, AnimatedCount } from "../ui/Stat"
