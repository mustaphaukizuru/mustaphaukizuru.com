/* eslint-disable react-refresh/only-export-components -- thin re-export barrel (canonical impl lives in components/ui) */
// Drawer · thin re-export — canonical implementation lives in components/ui/Drawer.jsx
export { default, Drawer, DrawerFooter } from "../ui/Drawer"
