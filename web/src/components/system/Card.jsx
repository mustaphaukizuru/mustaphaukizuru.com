/* eslint-disable react-refresh/only-export-components -- thin re-export barrel (canonical impl lives in components/ui) */
// Card · thin re-export — canonical implementation lives in components/ui/Card.jsx
export { default, Card, CardHeader, CardBody, CardFooter } from "../ui/Card"
