/* eslint-disable react-refresh/only-export-components -- thin re-export barrel (canonical impl lives in components/ui) */
// Button · thin re-export — canonical implementation lives in components/ui/Button.jsx
export { default, Button } from "../ui/Button"
