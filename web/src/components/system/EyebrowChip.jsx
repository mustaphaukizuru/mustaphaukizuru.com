/* eslint-disable react-refresh/only-export-components -- thin re-export barrel (canonical impl lives in components/ui) */
// EyebrowChip · thin re-export — canonical implementation lives in components/ui/EyebrowChip.jsx
export { default, EyebrowChip } from "../ui/EyebrowChip"
