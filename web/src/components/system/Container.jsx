/* eslint-disable react-refresh/only-export-components -- thin re-export barrel (canonical impl lives in components/ui) */
// Container · thin re-export — canonical implementation lives in components/ui/Container.jsx
export { default, Container } from "../ui/Container"
