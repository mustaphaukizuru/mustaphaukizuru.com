/* eslint-disable react-refresh/only-export-components -- thin re-export barrel (canonical impl lives in components/ui) */
// Badge · thin re-export — canonical implementation lives in components/ui/Badge.jsx
export { default, Badge, STATUS_TO_TONE } from "../ui/Badge"
