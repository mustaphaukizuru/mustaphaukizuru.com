/* eslint-disable react-refresh/only-export-components -- thin re-export barrel (canonical impl lives in components/ui) */
// Modal · thin re-export — canonical implementation lives in components/ui/Modal.jsx
export { default, Modal, ModalFooter } from "../ui/Modal"
