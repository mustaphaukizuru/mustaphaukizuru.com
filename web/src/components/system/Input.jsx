/* eslint-disable react-refresh/only-export-components -- thin re-export barrel (canonical impl lives in components/ui) */
// Input · thin re-export — canonical implementation lives in components/ui/Input.jsx
export { default, Input } from "../ui/Input"
