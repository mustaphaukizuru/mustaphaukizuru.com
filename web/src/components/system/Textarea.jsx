/* eslint-disable react-refresh/only-export-components -- thin re-export barrel (canonical impl lives in components/ui) */
// Textarea · thin re-export — canonical implementation lives in components/ui/Textarea.jsx
export { default, Textarea } from "../ui/Textarea"
