/* eslint-disable react-refresh/only-export-components -- thin re-export barrel (canonical impl lives in components/ui) */
// InlineBanner · thin re-export — canonical implementation lives in components/ui/InlineBanner.jsx
export { default, InlineBanner } from "../ui/InlineBanner"
