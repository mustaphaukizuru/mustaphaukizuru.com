/* eslint-disable react-refresh/only-export-components -- thin re-export barrel (canonical impl lives in components/ui) */
// EmptyState · thin re-export — canonical implementation lives in components/ui/EmptyStateSurface.jsx
export { default, EmptyState } from "../ui/EmptyStateSurface"
