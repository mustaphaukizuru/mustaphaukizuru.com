/* eslint-disable react-refresh/only-export-components -- thin re-export barrel (canonical impl lives in components/ui) */
// SectionHeader · thin re-export — canonical implementation lives in components/ui/SectionHeader.jsx
export { default, SectionHeader } from "../ui/SectionHeader"
